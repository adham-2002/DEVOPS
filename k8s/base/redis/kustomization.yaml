apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kubeshop

labels:
  - pairs:
       app: redis

resources:
  - deployment.yaml
  - secret.yaml
  - service.yaml
  - pvc.yaml
